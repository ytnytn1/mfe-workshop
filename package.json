{
  "private": true,
  "workspaces": {
    "packages": [
      "app1",
      "app2"
    ]
  },
  "scripts": {
    "start": "lerna run --parallel start",
    "build": "lerna run build",
    "build:prod": "lerna run build:production",
    "serve": "lerna run --parallel serve",
    "clean": "lerna run --parallel clean"
  },
  "devDependencies": {
    "lerna": "3.22.1"
  }
}
